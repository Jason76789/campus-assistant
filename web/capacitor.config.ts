import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourschool.campusassistant',
  appName: 'campus-assistant',
  webDir: 'dist'
};

export default config;
